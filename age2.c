#include <stdio.h>

int main() {
    int a;
    printf("Enter the age: ");
    scanf("%d", &a);
    printf("Age is %d\n", a); // Added format specifier %d and a newline character

    return 0;
}
